[project]
name = "telecom-network-reliability-platform"
version = "0.1.0"
description = "Production-style PySpark telecom data engineering platform and Spark learning lab."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

