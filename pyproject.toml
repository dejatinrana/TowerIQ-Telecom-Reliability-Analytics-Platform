[project]
name = "toweriq"
version = "0.1.0"
description = "TowerIQ: production-style PySpark telecom reliability analytics platform and Spark learning lab."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
